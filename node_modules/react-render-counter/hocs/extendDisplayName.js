'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

var _recompose = require('recompose');

exports.default = function (nameOrOptions) {
  return function (Component) {
    var options = (typeof nameOrOptions === 'undefined' ? 'undefined' : _typeof(nameOrOptions)) !== 'object' ? { name: nameOrOptions } : nameOrOptions;
    var name = options.name,
        _options$operation = options.operation,
        operation = _options$operation === undefined ? 'wrap' : _options$operation;


    var newDisplayName = void 0;
    switch (operation) {
      case 'wrap':
        {
          newDisplayName = (0, _recompose.wrapDisplayName)(Component, name);
          break;
        }
      case 'replace':
        {
          newDisplayName = name;
          break;
        }
      default:
        {
          newDisplayName = (0, _recompose.getDisplayName)(Component);
        }
    }
    return (0, _recompose.setDisplayName)(newDisplayName)(Component);
  };
};