'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _recompose = require('recompose');

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

exports.default = function (handlersArray) {
  if (Array.isArray(handlersArray)) {
    if (handlersArray.length > 1) {
      var hocs = handlersArray.map(function (handlers) {
        return (0, _recompose.withHandlers)(handlers);
      });
      return _recompose.compose.apply(undefined, _toConsumableArray(hocs));
    }
    return (0, _recompose.withHandlers)(handlersArray[0]);
  }
  return (0, _recompose.withHandlers)(handlersArray);
};